name: Release Branch CI

on:
  push:
    branches:
      - 'release/**'
    paths:
      - 'services/**'
      - 'gitops/**'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Force deploy to staging'
        required: true
        default: 'staging'
        type: choice
        options: [staging]

permissions:
  id-token: write
  contents: write
  security-events: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false  # never cancel an active staging deploy

env:
  AWS_REGION: us-east-1
  ECR_REGISTRY: ${{ secrets.ECR_REGISTRY }}
  ARGOCD_VERSION: v2.11.3

# ═══════════════════════════════════════════════════════════════════
#  RELEASE BRANCH PIPELINE
#  ──────────────────────────────────────────────────────────────────
#  Runs on every push to release/** branches.
#  Full scan → build → deploy to staging → full test suite → QA gate.
#
#  FLOW:
#    secret-scan ──┐
#    detect-changes─┤
#                   ├──► build & push ──► trivy-iac ──► gitops-update
#    sonarqube ─────┘         │
#                             └──► post-deploy-staging (smoke + integration
#                                  + E2E + performance + ZAP)
#                                       └──► qa-approval gate
#
#  OWNERSHIP
#  [DEVOPS OWNS]  All job wiring, k6 scripts, ZAP, ArgoCD sync
#  [DEV OWNS]     tests/integration/*.py, tests/e2e/*.spec.ts
# ═══════════════════════════════════════════════════════════════════

jobs:

  # ─────────────────────────────────────────────────────────────────
  # 0. COMPUTE TAGS  [DEVOPS OWNS]
  #    tag=staging-{sha}, deploy_env=staging
  # ─────────────────────────────────────────────────────────────────
  compute-tags:
    name: Compute image tag
    runs-on: ubuntu-latest
    outputs:
      tag:        ${{ steps.set.outputs.tag }}
      deploy_env: ${{ steps.set.outputs.deploy_env }}
    steps:
      - id: set
        run: |
          SHA=$(echo $GITHUB_SHA | cut -c1-8)
          echo "tag=staging-${SHA}"  >> $GITHUB_OUTPUT
          echo "deploy_env=staging"  >> $GITHUB_OUTPUT

  # ─────────────────────────────────────────────────────────────────
  # 1. SECRET SCAN  [DEVOPS OWNS]
  # ─────────────────────────────────────────────────────────────────
  secret-scan:
    name: Secret Scan (TruffleHog)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Secret scan
        uses: trufflesecurity/trufflehog@main
        with:
          path: ./
          base: ${{ github.event.before }}
          head: ${{ github.event.after }}
          extra_args: --only-verified

  # ─────────────────────────────────────────────────────────────────
  # 2. DETECT CHANGES  [DEVOPS OWNS]
  # ─────────────────────────────────────────────────────────────────
  detect-changes:
    name: Detect Changed Services
    runs-on: ubuntu-latest
    outputs:
      book:     ${{ steps.filter.outputs.book }}
      frontend: ${{ steps.filter.outputs.frontend }}
      search:   ${{ steps.filter.outputs.search }}
      user:     ${{ steps.filter.outputs.user }}
    steps:
      - uses: actions/checkout@v4

      - id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            book:     ['services/book/**']
            frontend: ['services/frontend/**']
            search:   ['services/search/**']
            user:     ['services/user/**']

  # ─────────────────────────────────────────────────────────────────
  # 3. SONARQUBE SAST  [DEVOPS OWNS]
  # ─────────────────────────────────────────────────────────────────
  sonarqube:
    name: SonarQube SAST
    runs-on: ubuntu-latest
    needs: [secret-scan]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: SonarQube scan
        uses: sonarsource/sonarqube-scan-action@master
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: https://sonarcloud.io
        with:
          args: >
            -Dsonar.projectKey=Surendra_Pandrinki_microservices-gitops-platform
            -Dsonar.organization=surendrakumar17
            -Dsonar.sources=services
            -Dsonar.exclusions=**/node_modules/**,**/*.test.*,**/vendor/**
            -Dsonar.branch.name=${{ github.ref_name }}

  # ─────────────────────────────────────────────────────────────────
  # 4. BUILD & PUSH  [DEVOPS OWNS]
  #    Trivy exit-code=0 on staging (warn only; prod is blocking).
  # ─────────────────────────────────────────────────────────────────
  build:
    name: Build & Push — ${{ matrix.service }}
    runs-on: ubuntu-latest
    needs: [detect-changes, compute-tags, secret-scan]
    strategy:
      fail-fast: false
      matrix:
        service: [book, frontend, search, user]
    steps:
      - uses: actions/checkout@v4

      - name: Check if service changed
        id: check
        run: |
          case "${{ matrix.service }}" in
            book)     echo "changed=${{ needs.detect-changes.outputs.book }}"     >> $GITHUB_OUTPUT ;;
            frontend) echo "changed=${{ needs.detect-changes.outputs.frontend }}" >> $GITHUB_OUTPUT ;;
            search)   echo "changed=${{ needs.detect-changes.outputs.search }}"   >> $GITHUB_OUTPUT ;;
            user)     echo "changed=${{ needs.detect-changes.outputs.user }}"     >> $GITHUB_OUTPUT ;;
          esac
          [[ "${{ github.event_name }}" == "workflow_dispatch" ]] && echo "changed=true" >> $GITHUB_OUTPUT

      - name: Configure AWS (OIDC)
        if: steps.check.outputs.changed == 'true'
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::635457411372:role/github-actions-ecr-role
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to ECR
        if: steps.check.outputs.changed == 'true'
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build & Push image
        if: steps.check.outputs.changed == 'true'
        run: |
          TAG=${{ needs.compute-tags.outputs.tag }}
          docker build \
            --label "git.sha=$GITHUB_SHA" \
            --label "git.branch=${GITHUB_REF##*/}" \
            -t $ECR_REGISTRY/${{ matrix.service }}:${TAG} \
            -t $ECR_REGISTRY/${{ matrix.service }}:latest-staging \
            ./services/${{ matrix.service }}
          docker push $ECR_REGISTRY/${{ matrix.service }}:${TAG}
          docker push $ECR_REGISTRY/${{ matrix.service }}:latest-staging

      - name: Trivy — table output (debug)
        if: steps.check.outputs.changed == 'true'
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ env.ECR_REGISTRY }}/${{ matrix.service }}:${{ needs.compute-tags.outputs.tag }}
          format: table
          exit-code: '0'
          severity: CRITICAL,HIGH
          scanners: vuln,secret

      - name: Trivy — SARIF (warn only on staging)
        if: steps.check.outputs.changed == 'true'
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ env.ECR_REGISTRY }}/${{ matrix.service }}:${{ needs.compute-tags.outputs.tag }}
          format: sarif
          output: trivy-image-${{ matrix.service }}.sarif
          severity: CRITICAL,HIGH
          exit-code: '0'
          ignore-unfixed: true

      - name: Upload Trivy SARIF
        if: steps.check.outputs.changed == 'true'
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: trivy-image-${{ matrix.service }}.sarif
          category: trivy-image-${{ matrix.service }}-${{ github.sha }}

  # ─────────────────────────────────────────────────────────────────
  # 5. TRIVY IaC SCAN  [DEVOPS OWNS]
  #    Scans gitops/ Helm + Terraform configs. Warn only on staging.
  # ─────────────────────────────────────────────────────────────────
  trivy-iac:
    name: Trivy — IaC / Helm / Terraform scan
    runs-on: ubuntu-latest
    needs: [secret-scan]
    steps:
      - uses: actions/checkout@v4

      - name: Trivy — IaC config scan (warn on staging)
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: config
          scan-ref: gitops/
          format: sarif
          output: trivy-iac.sarif
          severity: CRITICAL,HIGH,MEDIUM
          exit-code: '0'       # warn only; main pipeline blocks on this

      - name: Upload IaC SARIF
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: trivy-iac.sarif
          category: trivy-iac

  # ─────────────────────────────────────────────────────────────────
  # 6. GITOPS UPDATE  [DEVOPS OWNS]
  # ─────────────────────────────────────────────────────────────────
  gitops-update:
    name: GitOps — update staging image tags
    runs-on: ubuntu-latest
    needs: [build, detect-changes, sonarqube, trivy-iac, compute-tags]
    if: |
      !contains(needs.*.result, 'failure') &&
      !contains(needs.*.result, 'cancelled')
    steps:
      - uses: actions/checkout@v4

      - name: Install yq
        run: |
          sudo wget -q https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64 \
            -O /usr/local/bin/yq
          sudo chmod +x /usr/local/bin/yq

      - name: Update GitOps values (staging)
        run: |
          FILE=gitops/environments/staging/values.yaml
          TAG=${{ needs.compute-tags.outputs.tag }}
          update() {
            SERVICE=$1; CHANGED=$2
            if [[ "$CHANGED" == "true" || "${{ github.event_name }}" == "workflow_dispatch" ]]; then
              echo "  ↳ $SERVICE → $TAG"
              yq -i ".$SERVICE.tag = \"$TAG\"" $FILE
            fi
          }
          update frontend "${{ needs.detect-changes.outputs.frontend }}"
          update book     "${{ needs.detect-changes.outputs.book }}"
          update search   "${{ needs.detect-changes.outputs.search }}"
          update user     "${{ needs.detect-changes.outputs.user }}"

      - name: Commit & Push
        run: |
          TAG=${{ needs.compute-tags.outputs.tag }}
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add gitops/environments/staging/values.yaml
          git diff --staged --quiet && echo "Nothing to commit" && exit 0
          git commit -m "gitops(staging): bump image tags → $TAG [skip ci]"
          git push

  # ─────────────────────────────────────────────────────────────────
  # 7. POST-DEPLOY: STAGING  [FULL TEST SUITE]
  # ─────────────────────────────────────────────────────────────────
  post-deploy-staging:
    name: Staging — post-deploy tests
    runs-on: ubuntu-latest
    needs: [gitops-update]
    if: needs.gitops-update.result == 'success'
    steps:
      - uses: actions/checkout@v4

      - name: Cache ArgoCD CLI
        id: cache-argocd
        uses: actions/cache@v4
        with:
          path: /usr/local/bin/argocd
          key: argocd-${{ env.ARGOCD_VERSION }}

      - name: Install ArgoCD CLI
        if: steps.cache-argocd.outputs.cache-hit != 'true'
        run: |
          curl -sSL -o /usr/local/bin/argocd \
            https://github.com/argoproj/argo-cd/releases/download/${{ env.ARGOCD_VERSION }}/argocd-linux-amd64
          chmod +x /usr/local/bin/argocd

      - name: Wait for ArgoCD sync (staging)
        run: |
          argocd app wait bookstore-staging \
            --health --timeout 300 \
            --server ${{ secrets.ARGOCD_SERVER }} \
            --auth-token ${{ secrets.ARGOCD_TOKEN }} \
            --insecure

      # ── SMOKE TESTS  [DEVOPS OWNS] ───────────────────────────────
      - name: Smoke tests — health endpoints
        run: |
          BASE=${{ secrets.STAGING_URL }}
          check() {
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" --max-time 10 "$1")
            [[ "$STATUS" == "200" ]] && echo "  ✅ $1" || (echo "  ❌ $1 ($STATUS)" && exit 1)
          }
          check "$BASE/health"
          check "$BASE/api/books/health"
          check "$BASE/api/users/health"
          check "$BASE/api/search/health"

      # ── INTEGRATION TESTS  [SHARED] ──────────────────────────────
      # DevOps: pytest install. Dev: tests/integration/*.py
      - name: Install Python test dependencies
        run: pip install pytest requests --quiet

      - name: Integration tests
        run: |
          BASE=${{ secrets.STAGING_URL }}
          # Uncomment once tests/integration/ exists:
          # pytest tests/integration/ --base-url=$BASE -v --tb=short \
          #   --junit-xml=reports/integration-staging.xml
          echo "⚠️  PENDING: Add tests/integration/ (dev team)"

      # ── E2E TESTS  [SHARED] ──────────────────────────────────────
      # DevOps: Playwright install. Dev/QA: tests/e2e/*.spec.ts
      - name: Install Playwright + browser deps
        run: |
          npm install -g @playwright/test
          npx playwright install --with-deps chromium firefox webkit

      - name: E2E tests (Playwright)
        run: |
          # Uncomment once tests/e2e/*.spec.ts exist:
          # npx playwright test \
          #   --project=chromium --project=firefox \
          #   --reporter=html --output=reports/playwright
          echo "⚠️  PENDING: Add tests/e2e/*.spec.ts (QA/dev team)"

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-staging-report
          path: reports/playwright/
          if-no-files-found: ignore

      # ── PERFORMANCE TESTS  [DEVOPS OWNS] ────────────────────────
      # DevOps writes k6 scripts — no app knowledge needed.
      # Scripts: tests/performance/staging-smoke.js
      #          tests/performance/staging-load.js
      - name: Install k6
        run: |
          sudo gpg -k
          sudo gpg --no-default-keyring \
            --keyring /usr/share/keyrings/k6-archive-keyring.gpg \
            --keyserver hkp://keyserver.ubuntu.com:80 \
            --recv-keys C5AD17C747E3415A3642D57D77C6C491D6AC1D69
          echo "deb [signed-by=/usr/share/keyrings/k6-archive-keyring.gpg] \
            https://dl.k6.io/deb stable main" | sudo tee /etc/apt/sources.list.d/k6.list
          sudo apt-get update -qq && sudo apt-get install -y k6

      - name: Performance tests (k6) — smoke
        run: |
          # Uncomment once tests/performance/staging-smoke.js exists:
          # k6 run tests/performance/staging-smoke.js \
          #   --env BASE_URL=${{ secrets.STAGING_URL }} \
          #   --out json=reports/k6-smoke.json \
          #   --summary-trend-stats="avg,p(95),p(99)"
          echo "⚠️  TODO (DevOps): create tests/performance/staging-smoke.js"
          echo "     Target: 10 VUs × 30s, p95 < 500ms, error rate < 1%"

      - name: Performance tests (k6) — load
        run: |
          # Uncomment once tests/performance/staging-load.js exists:
          # k6 run tests/performance/staging-load.js \
          #   --env BASE_URL=${{ secrets.STAGING_URL }} \
          #   --out json=reports/k6-load.json
          echo "⚠️  TODO (DevOps): create tests/performance/staging-load.js"
          echo "     Target: ramp to 50 VUs over 5m, soak 10m, ramp down"

      # ── OWASP ZAP DAST  [DEVOPS OWNS] ───────────────────────────
      # DevOps owns config, rules file, thresholds.
      # Config: .zap/rules.tsv
      - name: OWASP ZAP — DAST baseline scan
        uses: zaproxy/action-baseline@v0.12.0
        with:
          target: ${{ secrets.STAGING_URL }}
          rules_file_name: .zap/rules.tsv
          cmd_options: '-a -j'
          fail_action: warn

      - name: Upload ZAP report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: zap-staging-report
          path: report_html.html

      - name: Staging test summary
        if: always()
        run: |
          echo "════════════════════════════════════════"
          echo "  STAGING POST-DEPLOY COMPLETE"
          echo ""
          echo "  [DEVOPS — done]"
          echo "  ✅ Smoke tests"
          echo "  ✅ OWASP ZAP DAST"
          echo "  ✅ k6 install + jobs wired (scripts TODO)"
          echo "  ✅ Playwright install + jobs wired (specs TODO)"
          echo ""
          echo "  [PENDING dev/QA]"
          echo "  ⚠️  Integration : tests/integration/*.py"
          echo "  ⚠️  E2E         : tests/e2e/*.spec.ts"
          echo "  ⚠️  k6 scripts  : DevOps to write (no blocker)"
          echo ""
          echo "  ⏳ Awaiting manual QA sign-off"
          echo "════════════════════════════════════════"

  # ─────────────────────────────────────────────────────────────────
  # 8. MANUAL QA GATE  [DEVOPS OWNS config]
  #    Configure: Settings → Environments → staging → required reviewers
  # ─────────────────────────────────────────────────────────────────
  qa-approval:
    name: "⏳ Manual QA sign-off (staging)"
    runs-on: ubuntu-latest
    needs: [post-deploy-staging]
    if: needs.post-deploy-staging.result == 'success'
    environment:
      name: staging
    steps:
      - name: QA gate passed
        run: |
          echo "✅ QA sign-off complete — release approved to merge to main"

# ═══════════════════════════════════════════════════════════════════
#  SECRETS REQUIRED
#    ECR_REGISTRY, ARGOCD_SERVER, ARGOCD_TOKEN
#    STAGING_URL, SONAR_TOKEN
#
#  DEV TEAM DELIVERABLES
#    tests/integration/       pytest integration tests
#    tests/e2e/*.spec.ts      Playwright user journeys
#
#  DEVOPS TODO (no dev dependency)
#    tests/performance/staging-smoke.js
#    tests/performance/staging-load.js
#    .zap/rules.tsv
# ═══════════════════════════════════════════════════════════════════